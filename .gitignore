# worker isolation dirs (created at runtime in the TARGET repos, but guard here too)
.worktrees/

# runtime artifacts
*.log
dispatch/STOP
dispatch/NIGHT-REPORT.md

# editor / OS noise
.DS_Store
*.swp
